build/
*.o
*.a
*.exe
gen_elf_tests
# Guest test binaries are generated by the scripts in tests/.
tests/bin/
tests/msvc/bin/
tests/dll/bin/
# The WebAssembly build is committed so the demo page can be served as-is.
!web/x86emu.js

# MSVC builds
build_msvc/
build_cmake/
x86emu.exe
gen_elf_tests.exe
*.obj
*.log
pybundle/

# The bundled CPython for the zero-setup browser demo is tracked on purpose,
# overriding the *.exe / build rules above.
!web/py/
!web/py/**
/pylinux/
